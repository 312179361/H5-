$(function () {
	/*
	alert($('input').length);
	// 选取第二个input元素
	// alert($('input').eq(1).val()); //语义不清晰
	// 通过type进行定位
	// alert($('input[type=password]').val()); //语义清晰了一些
	// 通过name属性来进行精确定位
	alert($('input[name=password]').val()); //语义更加清晰

	*/
	/*

	// :input 可以选择所有的表单元素，如input、select、button 、textarea等
	alert($(':input[name=city]').length);

	// :text 所有的单行文本框，
	alert($(':text').length);
	// :password 所有的密码框
	alert($(':password').length);
	// first() 和last()就是第一个和最后一个
	alert($(':radio[name=sex]').last().val());// 获取radio的name=sex的最后一个元素

	// :hidden 隐藏的元素，
	alert($('form :hidden').length); //form和:hidden中间有空格，即在form下隐藏的元素的个数

*/

	/*
	// 表单过滤器

	
	// :enabled 可以元素
	alert($('form :enabled').length);
	// :disabled 不可用元素
	alert($('form :disabled').length);
	// :checked 选定的元素，用于单元和复选
	alert($('form :checked').length);
	
	// :selected选取所有选中的元素 ，用于下拉列表。返回值的是option 
	alert($('form :selected').length);

	*/


});